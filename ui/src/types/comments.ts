export type CommentType = {
    attributes: {
        text: string;
        html?: string;
    };
    id: string;
};

export type CommentAttributes = {
    attributes: {
        text: string;
        html?: string;
    };
};
